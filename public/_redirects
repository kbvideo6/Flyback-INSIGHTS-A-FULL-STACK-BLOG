# Netlify SPA routing
# All routes should fall back to index.html so React Router can handle them.
/*    /index.html   200

# Optional: Redirect /admin to /admin/articles (or login if not authenticated)
# React Router handles the actual auth check.
/admin  /admin/articles  301
